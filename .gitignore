# Documentation - local only, not for GitHub
docs/

# dependencies
node_modules/
.pnp
.pnp.js

# next.js build
.next/
out/

# production
build/
dist/

# misc
.DS_Store
*.pem
.env*.local
.env.development
.env.test
.env.production

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# turbo
.turbo

# vercel
.vercel

# 本地 A2M 订单落盘（仅本地联调，不提交）
.a2m-orders/

# typescript
*.tsbuildinfo
next-env.d.ts
.vercel
dev/null

# prebuild 生成的客户端懒加载数据（构建时由 scripts/generate-books-data.mjs 生成）
public/data/books-*.json

# Temporary fresh clone used for l'x-path git repair (never commit)
getcreditworth-fresh/

# generate-content 脚本运行时产生的备份/干跑产物（非源码）
data/blog/posts.tsx.bak
data/blog/posts.tsx.dryrun

# 2026-09-20 一次性数据归档（已移出构建路径，详见 data/_archive/README.md）
# 注意：这些文件仍在仓库里，只是不再参与构建/部署（与 .vercelignore 保持一致）
data/_archive/
data/books-merged.json
data/seeds-*.json
data/extra-seeds.json
data/blog/append-posts.txt

# 存储优化期间的临时备份目录（不得提交）
_opt-backup-*/
